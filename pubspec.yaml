name: stockapp_mvp
description: Stock/Order MVP based on user's spec
version: 1.0.0+1
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  uuid: ^4.5.1
  intl: ^0.19.0
  # (추후 SQLite 전환 시)
  # sqflite: ^2.3.3
  # path_provider: ^2.1.4
  # path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sql/schema.sql
    - assets/sql/seed.sql

